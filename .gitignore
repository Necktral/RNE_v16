__pycache__/
*.py[cod]
.pytest_cache/
*:Zone.Identifier
.env
.env.*
!.env*.example
.venv/
venv/
.mypy_cache/
.ruff_cache/
.cache/
infra/docker/.env

# RNFE external models / local heavyweight artifacts
/mnt/
/rnfe_models/
*.gguf
*.safetensors
*.bin
*.pt
*.pth
*.onnx
*.ckpt

# HF / torch / model caches
.cache/huggingface/
.cache/torch/
huggingface/
hf_cache/
torch_cache/

# llama.cpp local builds if vendored accidentally
tools/llama.cpp/build*/
build-cuda/
build-cpu/

# Runtime-generated artifacts and local DBs — never commit
aeon_event_log.db
*.db
*.sqlite
*.sqlite3
rnfe_artifacts/
data/artifacts/
data/tmp/

# Scratch de scripts de corrida (sqlite + artefactos por episodio, regenerable)
data/reports/**/_work/

# Corpus del chunker de H-Net: ~460 MB de experiencia del organismo, REGENERABLE
# con `python -m lab.hnet_chunker.corpus`. Los shards no van al repo; el
# manifiesto (procedencia + sha256 por shard) y los reportes de evaluación SÍ.
data/hnet_corpus/*.jsonl
!data/hnet_corpus/manifest.json
!data/hnet_corpus/*_report.json
